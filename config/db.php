<?php
declare( strict_types = 1 );

/* MySQL Example
$app->inject( 'db', adoNewConnection( 'mysqli' ) );
$app->use( 'db' )->debug = true;
$app->use( 'db' )->connect(
	'127.0.0.1',
	'username',
	'password',
	'db-name'
);
 */
